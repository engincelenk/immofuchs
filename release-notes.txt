## 2026-06-09 (2)

### Bugfix: iOS Safari Rand-rechts behoben

**Problem**: iOS Safari ignoriert `overflow-x:hidden` auf `html/body` — führte zu horizontalem Überlauf und sichtbarem weißem Rand rechts (besonders als PWA/standalone).

**Fix**:
- Landing-Wrapper-`<div>`: `overflowX:"hidden"`, `position:"relative"`, `width:"100%"` hinzugefügt
- `.shell` (Haupt-App): `overflow-x:hidden`, `position:relative`, `width:100%` hinzugefügt
- Globale CSS (Landing + App): `width:100%`, `max-width:100%` auf `html,body`; `position:relative` auf `body`
- Betroffene Datei: src/App.jsx

---

## 2026-06-09

### Bugfix + UX: Preis/m², Mobile-Overflow, Eigennutzung-Removal, CSP-Fix

**Bugfix: Preis/m² Berechnung korrigiert**
- Preis/m² wurde fälschlicherweise mit `Kaufpreis + Garage/Stellplatz` berechnet
- Fix: Nur `Kaufpreis` als Basis (NBK-Berechnung nutzt weiterhin korrekt den Gesamtpreis)
- Betroffene Datei: src/App.jsx

**Bugfix: Mobiler Rand-Gap auf Landing-Page behoben**
- Rechts offener Rand auf Smartphones durch fehlendes `overflow-x:hidden` im Landing-Page-Style-Block
- Fix: `html,body{overflow-x:hidden}` in den Early-Return-Style-Block ergänzt
- Betroffene Datei: src/App.jsx (Landing-Page `<style>`-Block)

**Feature-Entfernung: Eigennutzung-Option aus Renditerechner entfernt**
- Eigennutzung-Toggle vollständig entfernt; Renditerechner ist für Kapitalanleger
- Ersetzt durch orangen Hinweis-Banner „Eigennutzung geplant?" mit Link zum Finanzierungsrechner
- `vermietet`-State, `immLeer`-Toggle, bedingter `useEffect` und alle bedingten Renderings entfernt
- `buildMP` wird jetzt immer aufgerufen
- FROZEN-Logik berührt: Nein

**UX: Banner-Platzierung & GESAMTSALDO-Karten korrigiert**
- „Eigennutzung geplant?"-Banner unter dem Mietrecht-Section-Header platziert (nicht davor)
- `detInfo` (Steuervorteil-Erklärung) aus GESAMTSALDO-OHNE-Karte entfernt → nur in MIT-Karte
- Doppelte ℹ️-Info-Box unterhalb der Karten entfernt
- Betroffene Datei: src/App.jsx (Detail-Komponente)

**Bugfix: CSP um region1.google-analytics.com erweitert**
- GA4 routet EU-Requests über `region1.google-analytics.com` — fehlte in `connect-src`
- Browser hat alle GA4-Requests geblockt; Tracking-Daten gingen verloren
- Fix: Domain in Content-Security-Policy ergänzt
- Betroffene Datei: index.html

---

## 2026-05-25

### UX: Klarere Labels für Nutzungsart & Belegung im Renditerechner

**Problem:** Die zwei Dropdowns „Immobilie vermieten?" und „Aktuell vermietet?" wirkten
redundant, da beide ähnlich klangen — obwohl sie konzeptionell verschiedene Dinge abfragen.

**Fix:** Labels und Optionswerte umbenannt (nur UI, keine Logikänderung):
- „Immobilie vermieten?" → **„Nutzungsart"**
  - „Ja, vermietet" → „Kapitalanlage (vermietet)"
  - „Nein / Eigennutzung" → „Eigennutzung"
- „Aktuell vermietet?" → **„Belegung"**
  - „Ja, aktuell vermietet" → „Aktuell vermietet"
  - „Nein, steht leer" → „Aktuell leer"

**Betroffene Dateien:** src/App.jsx (String-Map)
**FROZEN-Logik berührt:** Nein

---

## 2026-05-23 (2)

### Feature: Google Analytics 4 + Consent Mode v2

**Was:** GA4-Tracking eingebaut — zeigt welcher Rechner wie oft aufgerufen wird.

**Umsetzung:**
- `index.html` (src + dist): GA4-Script fest im `<head>` verankert (Consent Mode v2)
- Standard-Consent: `analytics_storage: denied` — kein Tracking ohne Zustimmung
- Nach Nutzer-Zustimmung im Cookie-Banner: `gtag('consent','update',...)` schaltet Tracking ein
- `App.jsx`: `useEffect` feuert `tab_view`-Event bei jedem Rechner-Wechsel
  → Sichtbar in GA4 unter Ereignisse → `tab_view` mit `tab_name` (z.B. „Renditerechner")
- CSP in beiden `index.html` um Google-Domains erweitert

**Measurement ID:** G-MQTYK65G3H
**DSGVO:** Consent Mode v2 (Googles offizieller Standard) — kein Cookie-Banner nötig für das Script selbst
**FROZEN-Logik berührt:** Nein
**Betroffene Dateien:** index.html, dist/index.html, src/App.jsx

---

## 2026-05-23

### UX: Leere Zahlenfelder setzen bei Verlassen automatisch auf 0

**Problem:** Löschte ein Nutzer den Wert in einem numerischen Eingabefeld vollständig,
blieb das Feld leer. Das wirkte unfertig und konnte in Edge-Cases zu NaN-Berechnungen führen.

**Fix:** `onBlur`-Handler in der zentralen `F`-Komponente erweitert:
  - Ist das Feld beim Verlassen leer → wird automatisch `0` gesetzt
  - Vorhandene Werte (Defaults oder Nutzereingaben) werden nicht angetastet
  - Nur numerische Felder (`type="number"`) betroffen
  - `readOnly`-, `date`- und `text`-Felder bleiben unverändert

**Reichweite:** Alle 4 Rechner (Rendite, Finanzierung, Mieterhöhung, Sanierung),
da alle numerischen Inputs die gemeinsame `F`-Komponente nutzen.

**Betroffene Dateien:** src/App.jsx (Zeile ~74)
**Geänderte Logik:** 5 Zeilen (nur hBlur)
**FROZEN-Logik berührt:** Nein (§558-BGB und Annuität unverändert)

---

## 2026-05-22

### Bugfix: Sonderumlage jetzt in Rendite-Berechnung eingebunden

**Problem:** Das Feld „Sonderumlage" im Renditerechner war nur mit dem Gesamtsaldo
(Verkaufsszenario) verbunden, hatte aber keinen Einfluss auf Brutto- und Nettorendite.
Nutzer, die eine Sonderumlage eingaben, sahen keine Änderung bei den Haupt-KPIs.

**Fix:** Die Sonderumlage fließt nun korrekt in die Investitionsbasis ein:
  - `gesamtInv = gKP + sonder`
  - Bruttorendite = Jahreskaltmiete / gesamtInv × 100
  - Nettorendite = (effektive Jahreskaltmiete − nicht umlagefähige Kosten) / gesamtInv × 100

**Fachliche Begründung:** Die Sonderumlage ist ein einmaliger Investitionsaufwand beim Kauf
(vergleichbar mit Kaufnebenkosten), der die Gesamtinvestition erhöht und damit die Rendite
korrekt senkt.

**Betroffene Dateien:** src/App.jsx (Zeile ~883)
**Geänderte Logik:** ~3 Zeilen
**FROZEN-Logik berührt:** Nein (§558-BGB und Annuität unverändert)
